{
  "name": "nitonabbc-form",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "biome format ./app",
    "format:write": "biome format --write ./app",
    "lint": "biome lint ./app",
    "lint:write": "biome lint --write ./app",
    "check": "biome check ./app",
    "check:write": "biome check --write ./app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nuxt": "^3.16.1",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@iconify/vue": "^4.3.0"
  }
}
