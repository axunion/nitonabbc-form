<header
	class="relative bg-gradient-to-br from-indigo-600 via-blue-600 to-purple-700 text-white"
>
	<div
		class="max-w-3xl mx-auto p-4 min-h-[50vh] flex items-center justify-center"
	>
		<div class="text-center">
			<div class="mb-4">
				<slot name="title" />
			</div>

			<div class="mb-6">
				<slot name="description" />
			</div>

			<slot name="additional" />
		</div>
	</div>

	<!-- Bottom Wave matching background -->
	<div class="absolute bottom-0 left-0 right-0">
		<svg
			class="w-full h-8 md:h-12"
			viewBox="0 0 1200 120"
			preserveAspectRatio="none"
		>
			<path
				d="M0,60 C300,30 900,90 1200,60 L1200,120 L0,120 Z"
				fill="#f0f4ff"></path>
		</svg>
	</div>
</header>
